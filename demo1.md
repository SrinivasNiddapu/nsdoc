# Personal Website Within one single prompt. 
## 1 Context
I want to build a booking platform where I can buy movie tickets and concert tickets, similar to "Book My Show", which is the most popular platform in India.

The way I want to build this platform is, I want to first start the project and only work on the navigation with empty screens.

Once the navigation is shorted, I want to work on each screen it has a clear objective, UI components, data attributes, interactions, animations, success validation. 

Assume we have like 10 pages. I want to do incremental screen-by-screen page builds and ensure a rich platform to build. 

(can you give me a rich componets)

Can you be my prompt engineer and help me build this platform? 

##  2 Single Markdown code format 

Always give me a single markdown code blocks, so it is easy to copy and paste. 

## 3 Setup Global CSS Team

Can you set up a global CSS theme? 

## 4 work on the prompt

Now let's work on the home page:
- Do the strategy and the build prompt
- What do you think are the list of components and elements that should be in home page, starting from header to footer?
- Give me a list of sections. How can each component should be what will be the behavior, for example carousel, sliding banners, banners, featured events, 